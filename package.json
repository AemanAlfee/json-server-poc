{
  "name": "json-server",
  "version": "0.0.1",
  "description": "A simple fake REST API server for testing and prototyping.",
  "keywords": ["jsonplaceholder", "json-server", "api testing", "endpoint"],
  "scripts": {
    "start": "json-server --watch data.json"
  },
  "dependencies": {
    "json-server": "0.16.3"
  },
  "engines": {
    "node": "14.x"
  },
  "license": "MIT"
}
